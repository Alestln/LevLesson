namespace LevLesson
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // Неправильные ответы
            /*double a = (3 + 5 * (6 + 3) - 8 * 3 + 1) / (21 + 50 / (3 + 4 * (1 + 2)));
            Console.WriteLine($"a={a}");
            
            double b = ((0.25 - 0.12) * 0.81 + 0.132 - 2.7 / 3.1) / (3 / 7 + 2.97 * (8.05 - 8.1 * 6.07));
            Console.WriteLine($"b={b}");*/

            // Операторы

            /*int x = 5, y = 7;
            int result = x + y; */// Оператор сложения +. Складывает два операнда.
            // x - левый операнд
            // y - правый операнд

            // Оператор присваивания =. Присваивает значение правого операнда левому операнду.

            // Операторы делятся на унарные (одинарные) и бинарные (двойные).
            // Унарные операторы имеют один операнд. Например, оператор инкремента ++, - (унарный минус).
            // Бинарные операторы имеют два операнда. Например, оператор сложения +, оператор вычитания - (бинарный минус).

            // Арифметические операторы
            // Оператор сложения +. Складывает два операнда.
            // Оператор вычитания -. Вычитает правый операнд из левого.
            // Оператор умножения *. Умножает два операнда.
            // Оператор деления /. Делит левый операнд на правый.
            // Оператор деления по модулю %. Возвращает остаток от деления левого операнда на правый.

            int result = -5 + 7 * 4 - (3 + 4 * 2); // 12

            // Приоритет операторов
            // 1. Скобки ()
            // 2. Унарные операторы +, - (унарный минус), ++, --
            // 3. Операторы умножения *, деления /, деления по модулю % - мультипликативные операторы
            // 4. Операторы сложения +, вычитания - (бинарный минус) - аддитивные операторы
            // 5. Операторы присваивания =, +=, -=, *=, /=, %=

            // Сокращенные операторы присваивания
            // += - сложение с присваиванием. a += b эквивалентно a = a + b
            // -= - вычитание с присваиванием. a -= b эквивалентно a = a - b
            // *= - умножение с присваиванием. a *= b эквивалентно a = a * b
            // /= - деление с присваиванием. a /= b эквивалентно a = a / b
            // %= - деление по модулю с присваиванием. a %= b эквивалентно a = a % b

            // Инкремент ++ и декремент --

            // Инкремент ++ увеличивает значение операнда на 1. Декремент -- уменьшает значение операнда на 1.
            // Например: x++; эквивалентно x = x + 1; x--; эквивалентно x = x - 1;
            // Инкремент делится на префиксный и постфиксный.
            // Префиксный инкремент ++x сначала увеличивает значение x на 1, а затем возвращает новое значение x.
            // Постфиксный инкремент x++ сначала возвращает текущее значение x, а затем увеличивает значение x на 1.

            /*int x = 5;
            int y = ++x*/; // y=6, x=6

            /*int x = 5;
            int y = x++;*/ // y=5, x=6

            // Целочисленное деление
            int x = 5, y = 2; // Типы данных должны быть целочисленными

            double res = x / (double)y; // приведение типов
            res = x / 2.0; // приведение типов

            Console.WriteLine(res);

            // int / int = int
            // int / double = double
            // double / int = double
            // double / double = double

            // Домашнее задание
            /*
             * 1. Прочитать про приведение типов.
             * 2. Прочитать про безопасное и небезопасное приведение типов.
             * 3. Прочитать про явное и неявное приведение типов.
             * 4. Переделать примеры, в которых неправильные ответы.
             * 5. Решить задачи:
             * а) Найти площадь прямоугольника, если известны его длина и ширина.
             * б) Найти площадь круга, если известен его радиус.
             * в) Найти площадь треугольника, если известны его основание и высота.
             * г) Найти площадь треугольника по формуле Герона, если известны длины его сторон.
             * 
             * Для решения задач использовать переменные для хранения входных данных и результата.
             */
        }
    }
}
